from django.apps import AppConfig


class SqlsafeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.sqlsafe"
    verbose_name = "SQL Safe — Modelo Semántico"
